diff --git a/apps/api/script/promote-job.js b/apps/api/script/promote-job.js
new file mode 100644
index 0000000..e9297d7
--- /dev/null
+++ b/apps/api/script/promote-job.js
@@ -0,0 +1,85 @@
+import 'dotenv/config';
+import { Queue } from 'bullmq';
+import IORedis from 'ioredis';
+
+const args = process.argv.slice(2);
+if (args.length < 1) {
+  console.log('Usage: node promote-job.js <queueName> <jobId>');
+  console.log('       node promote-job.js <queueName> --list');
+  console.log('       node promote-job.js --list-queues');
+  console.log('Example: node promote-job.js payment grace_expiry_12345');
+  console.log('Example: node promote-job.js payment --list');
+  process.exit(1);
+}
+
+const queueNameOrCommand = args[0];
+const jobId = args[1];
+
+const connection = new IORedis(process.env.REDIS_URL, {
+  maxRetriesPerRequest: null
+});
+
+async function run() {
+  try {
+    if (queueNameOrCommand === '--list-queues') {
+      const keys = await connection.keys('bull:*:id');
+      console.log('\n=== Available BullMQ Queues in Redis ===');
+      if (keys.length === 0) {
+        console.log('No queues found.');
+      } else {
+        const queues = keys.map(k => k.split(':')[1]);
+        queues.forEach(q => console.log(`- ${q}`));
+      }
+      return;
+    }
+
+    const queueName = queueNameOrCommand;
+    const queue = new Queue(queueName, { connection });
+
+    if (jobId === '--list') {
+      const jobs = await queue.getJobs(['delayed']);
+      console.log(`\n=== Delayed Jobs in Queue: '${queueName}' ===`);
+      if (jobs.length === 0) {
+        console.log('No delayed jobs found.');
+      } else {
+        jobs.forEach(j => {
+          console.log(`- JobID: ${j.id} | Name: ${j.name}`);
+          console.log(`  Data:`, j.data);
+        });
+      }
+      await queue.close();
+      return;
+    }
+
+    if (!jobId) {
+      console.error('Γ¥î Please provide a jobId or use --list');
+      await queue.close();
+      process.exit(1);
+    }
+
+    const job = await queue.getJob(jobId);
+    if (!job) {
+      console.error(`Γ¥î Job with ID ${jobId} not found in queue ${queueName}.`);
+      console.log(`Tip: Run "node promote-job.js ${queueName} --list" to see all delayed jobs and their exact IDs.`);
+      await queue.close();
+      process.exit(1);
+    }
+
+    const state = await job.getState();
+    if (state !== 'delayed') {
+      console.error(`Γ¥î Job ${jobId} is in state '${state}'. Only 'delayed' jobs can be promoted.`);
+      await queue.close();
+      process.exit(1);
+    }
+
+    await job.promote();
+    console.log(`Γ£à Successfully promoted job ${jobId}. It should run immediately.`);
+    await queue.close();
+  } catch (error) {
+    console.error('Γ¥î Error:', error);
+  } finally {
+    await connection.quit();
+  }
+}
+
+run();
diff --git a/apps/api/script/trigger-sweeper.js b/apps/api/script/trigger-sweeper.js
new file mode 100644
index 0000000..cb9bc74
--- /dev/null
+++ b/apps/api/script/trigger-sweeper.js
@@ -0,0 +1,43 @@
+import 'dotenv/config';
+import { Queue } from 'bullmq';
+import IORedis from 'ioredis';
+
+const args = process.argv.slice(2);
+if (args.length < 2) {
+  console.log('Usage: node trigger-sweeper.js <queueName> <jobName>');
+  console.log('Available Sweepers (verify names in queue.js):');
+  console.log('  - payment: grace-period-sweeper, payment-sweeper');
+  console.log('  - webhook: webhook-reaper');
+  console.log('  - fulfillment: fulfillment-sweeper');
+  console.log('  - dispute: dispute-expiry-sweeper');
+  console.log('Example: node trigger-sweeper.js dispute dispute-expiry-sweeper');
+  process.exit(1);
+}
+
+const [queueName, jobName] = args;
+
+const connection = new IORedis(process.env.REDIS_URL, {
+  maxRetriesPerRequest: null
+});
+
+const queue = new Queue(queueName, { connection });
+
+async function trigger() {
+  try {
+    // ΓÜá∩╕Å L╞»U ├¥: Script n├áy truyß╗ün data l├á {} (rß╗ùng)
+    // Ph├╣ hß╗úp cho ─æa sß╗æ Sweeper/Reaper v├¼ ch├║ng tß╗▒ query DB.
+    // Nß║┐u file *.worker.js cß╗ºa bß║ín check `job.data.someField` ─æß╗â chß║íy logic,
+    // script n├áy c├│ thß╗â fail hoß║╖c no-op. H├úy kiß╗âm tra logic xß╗¡ l├╜ tr╞░ß╗¢c khi chß║íy.
+    const jobId = `manual-trigger-${Date.now()}`;
+    await queue.add(jobName, {}, { jobId });
+    console.log(`Γ£à Successfully triggered sweeper job '${jobName}' on queue '${queueName}'.`);
+    console.log(`Job ID: ${jobId}`);
+  } catch (error) {
+    console.error('Γ¥î Error triggering sweeper:', error);
+  } finally {
+    await queue.close();
+    await connection.quit();
+  }
+}
+
+trigger();
diff --git a/apps/web/src/components/layout/Header.jsx b/apps/web/src/components/layout/Header.jsx
index 3967d89..f90ce5a 100644
--- a/apps/web/src/components/layout/Header.jsx
+++ b/apps/web/src/components/layout/Header.jsx
@@ -59,14 +59,6 @@ export function Header() {
                 >
                   My Bids
                 </NavLink>
-                <NavLink
-                  to="/watchlist"
-                  className={({ isActive }) =>
-                    `header__nav-link${isActive ? ' header__nav-link--active' : ''}`
-                  }
-                >
-                  Watchlist
-                </NavLink>
               </>
             )}
           </nav>
diff --git a/apps/web/src/features/auctions/pages/AuctionBrowsePage.css b/apps/web/src/features/auctions/pages/AuctionBrowsePage.css
index f2d8463..e85a793 100644
--- a/apps/web/src/features/auctions/pages/AuctionBrowsePage.css
+++ b/apps/web/src/features/auctions/pages/AuctionBrowsePage.css
@@ -9,43 +9,47 @@
 .browse-layout {
   display: flex;
   flex-direction: column;
+  max-width: var(--max-content-width, 1440px);
+  margin: 0 auto;
+  padding: 0 var(--space-6);
+  width: 100%;
+  box-sizing: border-box;
 }
 
 .browse-main {
-  padding: var(--space-4) var(--space-6);
-  max-width: 1400px;
+  padding: var(--space-4) 0;
   width: 100%;
-  margin: 0 auto;
-  box-sizing: border-box;
 }
 
 .status-tabs {
   display: flex;
-  gap: var(--space-6);
+  flex-direction: row;
+  gap: 20px;
   margin-bottom: var(--space-6);
-  border-bottom: 1px solid var(--color-surface-raised);
+  border-bottom: 1px solid var(--color-border);
 }
 
 .status-tab {
   background: none;
   border: none;
-  padding: 0 0 var(--space-2) 0;
+  padding: 0 0 10px 0;
   margin-bottom: -1px;
   color: var(--color-text-secondary);
-  font-weight: 500;
-  font-size: var(--font-size-md);
+  font-family: var(--font-body);
+  font-size: var(--text-sm, 13px);
   cursor: pointer;
   border-bottom: 2px solid transparent;
-  transition: all 0.2s ease;
+  transition: color 0.15s, border-color 0.15s;
 }
 
 .status-tab:hover {
-  color: var(--color-text);
+  color: var(--color-text-primary);
 }
 
 .status-tab.active {
-  color: var(--color-primary);
-  border-bottom-color: var(--color-primary);
+  color: var(--color-text-primary);
+  font-weight: 500;
+  border-bottom-color: var(--color-brand);
 }
 
 .browse-grid {
diff --git a/apps/web/src/features/auctions/pages/AuctionBrowsePage.jsx b/apps/web/src/features/auctions/pages/AuctionBrowsePage.jsx
index 73b17ac..43b1abb 100644
--- a/apps/web/src/features/auctions/pages/AuctionBrowsePage.jsx
+++ b/apps/web/src/features/auctions/pages/AuctionBrowsePage.jsx
@@ -98,8 +98,8 @@ export function AuctionBrowsePage() {
               Active Auctions
             </button>
             <button 
-              className={`status-tab ${filters.status === 'draft' ? 'active' : ''}`}
-              onClick={() => handleStatusChange('draft')}
+              className={`status-tab ${filters.status === 'scheduled' ? 'active' : ''}`}
+              onClick={() => handleStatusChange('scheduled')}
             >
               Upcoming
             </button>
diff --git a/apps/web/src/features/auctions/pages/AuctionDetailPage.css b/apps/web/src/features/auctions/pages/AuctionDetailPage.css
index 2070d4d..7ec228d 100644
--- a/apps/web/src/features/auctions/pages/AuctionDetailPage.css
+++ b/apps/web/src/features/auctions/pages/AuctionDetailPage.css
@@ -62,6 +62,7 @@
 .auction-detail-main-image-container {
   width: 100%;
   aspect-ratio: 4/3;
+  max-height: 480px;
   background-color: var(--color-surface-raised);
   position: relative;
 }
diff --git a/apps/web/src/features/dashboard/pages/DashboardHome.css b/apps/web/src/features/dashboard/pages/DashboardHome.css
new file mode 100644
index 0000000..69cb80f
--- /dev/null
+++ b/apps/web/src/features/dashboard/pages/DashboardHome.css
@@ -0,0 +1,121 @@
+/* apps/web/src/features/dashboard/pages/DashboardHome.css */
+
+.dashboard-home-page {
+  display: flex;
+  flex-direction: column;
+  gap: var(--space-6);
+}
+
+.metric-cards-grid {
+  display: grid;
+  grid-template-columns: repeat(4, 1fr);
+  gap: var(--space-4);
+}
+
+.metric-card {
+  background: var(--color-surface);
+  border: 1px solid var(--color-border);
+  border-radius: var(--radius-md);
+  padding: 12px;
+  display: flex;
+  flex-direction: column;
+  gap: var(--space-2);
+}
+
+.metric-label {
+  font-family: var(--font-body);
+  font-size: var(--text-xs, 11px);
+  color: var(--color-text-secondary);
+}
+
+.metric-value {
+  font-family: var(--font-mono);
+  font-size: var(--text-lg, 18px);
+  font-weight: 600;
+  color: var(--color-text-primary);
+}
+
+.skeleton-card {
+  height: 64px;
+  background: var(--color-surface-raised);
+  border-radius: var(--radius-md);
+}
+
+.recent-activity-panel {
+  background: var(--color-surface);
+  border: 1px solid var(--color-border);
+  border-radius: var(--radius-md);
+  padding: var(--space-4);
+}
+
+.recent-activity-panel h3 {
+  font-family: var(--font-body);
+  font-size: var(--text-sm, 13px);
+  font-weight: 500;
+  margin-top: 0;
+  margin-bottom: var(--space-4);
+  color: var(--color-text-primary);
+}
+
+.activity-list {
+  list-style: none;
+  padding: 0;
+  margin: 0;
+  display: flex;
+  flex-direction: column;
+  gap: var(--space-3);
+}
+
+.activity-item {
+  display: flex;
+  flex-direction: row;
+  align-items: flex-start;
+  gap: var(--space-3);
+}
+
+.activity-icon {
+  font-size: 14px;
+}
+
+.activity-text {
+  font-family: var(--font-body);
+  font-size: var(--text-sm, 13px);
+  color: var(--color-text-primary);
+  flex: 1;
+}
+
+.activity-time {
+  font-family: var(--font-body);
+  font-size: var(--text-xs, 11px);
+  color: var(--color-text-disabled);
+  white-space: nowrap;
+}
+
+.activity-empty {
+  font-family: var(--font-body);
+  font-size: var(--text-sm, 13px);
+  color: var(--color-text-secondary);
+}
+
+.skeleton-line {
+  height: 20px;
+  background: var(--color-surface-raised);
+  border-radius: var(--radius-sm);
+  margin-bottom: var(--space-3);
+}
+
+.empty-state {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+  padding: var(--space-10) 0;
+  text-align: center;
+  gap: var(--space-4);
+}
+
+.empty-state p {
+  color: var(--color-text-secondary);
+  font-family: var(--font-body);
+  font-size: var(--text-base, 16px);
+}
diff --git a/apps/web/src/features/dashboard/pages/DashboardHome.jsx b/apps/web/src/features/dashboard/pages/DashboardHome.jsx
index faeb2e1..5bd9e50 100644
--- a/apps/web/src/features/dashboard/pages/DashboardHome.jsx
+++ b/apps/web/src/features/dashboard/pages/DashboardHome.jsx
@@ -1,8 +1,112 @@
+import { useFetch } from '../../../core/hooks/useFetch';
+import { useAuth } from '../../../core/context/AuthContext';
+import { Link } from 'react-router-dom';
+import './DashboardHome.css';
+
 export function DashboardHome() {
+  const { user } = useAuth();
+
+  // Fetch summary data
+  const { data: activeBidsRes, isLoading: loadingActive } = useFetch('/auctions?bidder_id=me&status=active');
+  const { data: winningBidsRes, isLoading: loadingWinning } = useFetch('/bids?user_id=me&is_winning=true');
+  const { data: wonAuctionsRes, isLoading: loadingWon } = useFetch('/auctions?winner_id=me'); // Assuming winner_id exists based on pattern
+  const { data: openDisputesRes, isLoading: loadingDisputes } = useFetch('/disputes?user_id=me&status=open'); // Assuming this exists
+
+  // Fetch recent activity (notifications)
+  const { data: notificationsRes, isLoading: loadingNotifs } = useFetch('/notifications?limit=8');
+
+  const isLoading = loadingActive || loadingWinning || loadingWon || loadingDisputes || loadingNotifs;
+  const isNewUser = !isLoading && 
+    activeBidsRes?.data?.items?.length === 0 && 
+    winningBidsRes?.data?.length === 0 && 
+    wonAuctionsRes?.data?.items?.length === 0 && 
+    (!notificationsRes?.data || notificationsRes?.data?.length === 0);
+
+  const formatRelativeTime = (isoString) => {
+    if (!isoString) return '';
+    const diff = Date.now() - new Date(isoString).getTime();
+    const hours = Math.floor(diff / (1000 * 60 * 60));
+    if (hours < 24) return `${hours}h ago`;
+    return `${Math.floor(hours / 24)}d ago`;
+  };
+
+  const getActivityIcon = (type) => {
+    switch (type) {
+      case 'bid:outbid': return '≡ƒö┤'; // Fallback icon, actual icon should be CSS/SVG
+      case 'auction:ended': return '≡ƒÅü';
+      case 'auction:won': return '≡ƒÅå';
+      default: return '≡ƒöö';
+    }
+  };
+
+  if (isLoading) {
+    return (
+      <div className="dashboard-home-page">
+        <div className="metric-cards-grid">
+          {[1, 2, 3, 4].map(i => <div key={i} className="skeleton-card"></div>)}
+        </div>
+        <div className="recent-activity-panel">
+          <div className="skeleton-line"></div>
+          <div className="skeleton-line"></div>
+          <div className="skeleton-line"></div>
+        </div>
+      </div>
+    );
+  }
+
+  if (isNewUser) {
+    return (
+      <div className="dashboard-home-page">
+        <div className="empty-state">
+          <p>Ch╞░a c├│ hoß║ít ─æß╗Öng n├áo. Bß║»t ─æß║ºu kh├ím ph├í Market!</p>
+          <Link to="/" className="btn btn-primary btn-lg">Go to Market</Link>
+        </div>
+      </div>
+    );
+  }
+
+  const activeBidsCount = activeBidsRes?.data?.items?.length || 0;
+  const winningBidsCount = winningBidsRes?.data?.length || 0;
+  const wonAuctionsCount = wonAuctionsRes?.data?.items?.length || 0;
+  const openDisputesCount = openDisputesRes?.data?.length || 0;
+  const notifications = notificationsRes?.data || [];
+
   return (
     <div className="dashboard-home-page">
-      <h1>Dashboard Home</h1>
-      <p>Welcome to your dashboard overview.</p>
+      <div className="metric-cards-grid">
+        <div className="metric-card">
+          <span className="metric-label">Active Bids</span>
+          <span className="metric-value">{activeBidsCount}</span>
+        </div>
+        <div className="metric-card">
+          <span className="metric-label">Winning Bids</span>
+          <span className="metric-value">{winningBidsCount}</span>
+        </div>
+        <div className="metric-card">
+          <span className="metric-label">Won Auctions</span>
+          <span className="metric-value">{wonAuctionsCount}</span>
+        </div>
+        <div className="metric-card">
+          <span className="metric-label">Open Disputes</span>
+          <span className="metric-value">{openDisputesCount}</span>
+        </div>
+      </div>
+
+      <div className="recent-activity-panel">
+        <h3>Recent Activity</h3>
+        <ul className="activity-list">
+          {notifications.map((notif) => (
+            <li key={notif.id} className="activity-item">
+              <span className="activity-icon">{getActivityIcon(notif.type)}</span>
+              <span className="activity-text">{notif.message}</span>
+              <span className="activity-time">{formatRelativeTime(notif.created_at)}</span>
+            </li>
+          ))}
+          {notifications.length === 0 && (
+            <li className="activity-empty">No recent activity.</li>
+          )}
+        </ul>
+      </div>
     </div>
   );
 }
diff --git a/scratch_get_columns.js b/scratch_get_columns.js
new file mode 100644
index 0000000..e42fb91
--- /dev/null
+++ b/scratch_get_columns.js
@@ -0,0 +1,19 @@
+import dotenv from 'dotenv';
+dotenv.config({ path: 'apps/api/.env' });
+import pg from 'pg';
+const { Pool } = pg;
+const pool = new Pool({ connectionString: process.env.DATABASE_URL });
+async function test() {
+  const res = await pool.query(`
+    SELECT table_name, column_name 
+    FROM information_schema.columns 
+    WHERE table_schema = 'public' 
+      AND data_type = 'timestamp without time zone'
+  `);
+  for (const row of res.rows) {
+    if (row.table_name === 'pgmigrations') continue;
+    console.log(`pgm.alterColumn('${row.table_name}', '${row.column_name}', { type: 'timestamptz' });`);
+  }
+  process.exit(0);
+}
+test();
