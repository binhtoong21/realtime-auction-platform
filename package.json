{
  "name": "realtime-auction-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:api": "npm start -w @auction/api",
    "dev:api": "npm run dev -w @auction/api",
    "dev:web": "npm run dev -w @auction/web",
    "db:seed": "npm run db:seed -w @auction/api"
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3"
  }
}
